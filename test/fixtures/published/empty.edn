;; FIXTURE: a manifest that exists and publishes nothing.
;; EXPECTED: :ok, no routes, the site renders its own sections. Identical in
;; effect to an absent manifest — see the reader rules table in
;; open-hax.website.law.published-manifest.
;;
;; PROVENANCE: written by the Knoxx static-site publication target adapter
;; (card knoxx-publication-static-site-target; expected namespace
;; knoxx.backend.infra.publication-target-static-site, sibling of
;; knoxx.backend.infra.publication-target-memory). When these fixtures and a
;; real manifest diverge, that adapter is where to look.
{:manifest/version 1
 :manifest/generated-at "2026-08-21T18:04:11Z"
 :manifest/routes []}
