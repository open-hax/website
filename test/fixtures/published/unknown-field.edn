;; FIXTURE: a writer newer than this reader, adding fields it did not declare.
;; EXPECTED: :ok, one route. The unknown fields are IGNORED — never fatal, and
;; dropped from the decoded route so nothing downstream can come to depend on
;; a field this reader has not declared.
;;
;; PROVENANCE: knoxx.backend.infra.publication-target-static-site
;; (card knoxx-publication-static-site-target).
{:manifest/version 1
 :manifest/generated-at "2026-08-21T18:04:11Z"
 :manifest/writer "knoxx/static-site@future"
 :manifest/routes
 [{:route/path "/es/notes/hello"
   :route/locale :es
   :route/document "notes/hello"
   :route/artifact "artifacts/notes/hello/es/rev-7f3a91c.html"
   :route/media-type "text/html"
   :route/experiment true
   :route/word-count 412}]}
